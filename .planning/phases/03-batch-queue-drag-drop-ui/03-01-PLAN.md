---
phase: 03-batch-queue-drag-drop-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SeratoDynamicAnalyzer/Models/TrackItem.swift
  - SeratoDynamicAnalyzer/ViewModels/BatchViewModel.swift
  - SeratoDynamicAnalyzer/Views/BatchHeaderView.swift
  - SeratoDynamicAnalyzer/Views/BatchQueueView.swift
  - SeratoDynamicAnalyzer/Views/TrackRowView.swift
  - SeratoDynamicAnalyzer/ContentView.swift
  - SeratoDynamicAnalyzer/PythonBridge.swift
  - SeratoDynamicAnalyzer/AnalysisViewModel.swift
autonomous: true
requirements:
  - INPUT-01
  - INPUT-02
  - BATCH-01
  - BATCH-02
  - UI-01

must_haves:
  truths:
    - "Dropping individual MP3/AIFF/WAV files onto the window adds them as Pending rows in the queue"
    - "Dropping a folder recursively discovers all supported audio files and adds them as Pending rows"
    - "Clicking 'Analyze All' starts analysis; up to 4 workers process tracks in parallel"
    - "Each row shows a status pill that transitions Pending → Analyzing → Done/Failed as the track is processed"
    - "Batch header shows 'X of N tracks complete' with a live linear ProgressView"
    - "The app builds and runs without errors after AnalysisViewModel is deleted"
  artifacts:
    - path: "SeratoDynamicAnalyzer/Models/TrackItem.swift"
      provides: "Per-track model: TrackStatus enum, TrackItem class, @Published status/progress/result/errorMessage/analysisDuration/isExpanded/bpmMinOverride/bpmMaxOverride"
      contains: "class TrackItem: ObservableObject"
    - path: "SeratoDynamicAnalyzer/ViewModels/BatchViewModel.swift"
      provides: "Queue owner: tracks array, worker pool (1-4 PythonBridge instances), dispatchNextPendingTracks(), startBatch(), addDroppedURL()"
      contains: "class BatchViewModel: ObservableObject"
    - path: "SeratoDynamicAnalyzer/Views/BatchHeaderView.swift"
      provides: "Sticky header: progress counter, linear ProgressView, global BPM Steppers, Dry Run toggle, Analyze All CTA"
      contains: "struct BatchHeaderView: View"
    - path: "SeratoDynamicAnalyzer/Views/BatchQueueView.swift"
      provides: "Primary content area: List of TrackRowView, onDrop multi-file handler, folder scan, compact drop strip, Choose Files NSOpenPanel button"
      contains: "struct BatchQueueView: View"
    - path: "SeratoDynamicAnalyzer/Views/TrackRowView.swift"
      provides: "Per-row view: StatusPillView, filename, per-track ProgressView (Analyzing), BPM/markers (Done), error summary (Failed)"
      contains: "struct TrackRowView: View"
    - path: "SeratoDynamicAnalyzer/ContentView.swift"
      provides: "App root: BatchViewModel @StateObject, BatchHeaderView + BatchQueueView composition, Serato alert, .task worker startup"
      contains: "@StateObject private var viewModel = BatchViewModel()"
  key_links:
    - from: "SeratoDynamicAnalyzer/Views/BatchQueueView.swift"
      to: "SeratoDynamicAnalyzer/ViewModels/BatchViewModel.swift"
      via: "addDroppedURL(_:) called from onDrop completion handler"
      pattern: "addDroppedURL"
    - from: "SeratoDynamicAnalyzer/ViewModels/BatchViewModel.swift"
      to: "SeratoDynamicAnalyzer/PythonBridge.swift"
      via: "bridge.analyzeStream() in dispatchNextPendingTracks()"
      pattern: "analyzeStream"
    - from: "SeratoDynamicAnalyzer/Views/TrackRowView.swift"
      to: "SeratoDynamicAnalyzer/Models/TrackItem.swift"
      via: "@ObservedObject var item: TrackItem"
      pattern: "@ObservedObject"
---

<objective>
Transform the Phase 2 single-track UI into a working batch queue. After this plan:
- The user can drop any number of audio files or folders onto the window
- All dropped files appear as Pending rows in a scrollable queue
- Clicking "Analyze All" starts 1–4 parallel Python workers that process the queue
- Every row transitions Pending → Analyzing → Done/Failed with a live status pill
- The batch header shows "X of N tracks complete" with a linear progress bar

Purpose: This is the vertical core of Phase 3 — every other feature (accordion detail, BPM override, cancel) builds on this working end-to-end slice.

Output: 5 new Swift files, ContentView.swift and PythonBridge.swift modified, AnalysisViewModel.swift deleted. The Xcode project's build phase file list must include all new files.
</objective>

<execution_context>
@/Users/andreasmrogenda/Claude Projekte/Serato Dynamic Analyzer/.planning/phases/03-batch-queue-drag-drop-ui/../../../.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/andreasmrogenda/Claude Projekte/Serato Dynamic Analyzer/.planning/ROADMAP.md
@/Users/andreasmrogenda/Claude Projekte/Serato Dynamic Analyzer/.planning/phases/03-batch-queue-drag-drop-ui/03-CONTEXT.md
@/Users/andreasmrogenda/Claude Projekte/Serato Dynamic Analyzer/.planning/phases/03-batch-queue-drag-drop-ui/03-RESEARCH.md
@/Users/andreasmrogenda/Claude Projekte/Serato Dynamic Analyzer/.planning/phases/03-batch-queue-drag-drop-ui/03-PATTERNS.md
@/Users/andreasmrogenda/Claude Projekte/Serato Dynamic Analyzer/.planning/phases/03-batch-queue-drag-drop-ui/03-UI-SPEC.md
@/Users/andreasmrogenda/Claude Projekte/Serato Dynamic Analyzer/SeratoDynamicAnalyzer/Models/AnalysisResult.swift
@/Users/andreasmrogenda/Claude Projekte/Serato Dynamic Analyzer/SeratoDynamicAnalyzer/PythonBridge.swift
@/Users/andreasmrogenda/Claude Projekte/Serato Dynamic Analyzer/SeratoDynamicAnalyzer/AnalysisViewModel.swift
@/Users/andreasmrogenda/Claude Projekte/Serato Dynamic Analyzer/SeratoDynamicAnalyzer/ContentView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: TrackItem model, BatchViewModel with worker pool, and PythonBridge.terminate()</name>
  <files>
    SeratoDynamicAnalyzer/Models/TrackItem.swift,
    SeratoDynamicAnalyzer/ViewModels/BatchViewModel.swift,
    SeratoDynamicAnalyzer/PythonBridge.swift
  </files>

  <read_first>
    - SeratoDynamicAnalyzer/Models/AnalysisResult.swift — WorkerEvent enum and AnalysisResult struct; both reused unchanged
    - SeratoDynamicAnalyzer/AnalysisViewModel.swift — @MainActor class pattern, startWorker(), isSeratoRunning(), analyzeStream loop; BatchViewModel replaces this 1:1
    - SeratoDynamicAnalyzer/PythonBridge.swift — actor interface: startWorker(), waitForReady(), analyzeStream(); add terminate() here
    - .planning/phases/03-batch-queue-drag-drop-ui/03-PATTERNS.md — BatchViewModel pattern section (lines 89–200), TrackItem pattern section (lines 25–58), PythonBridge addition section (lines 540–554)
    - .planning/phases/03-batch-queue-drag-drop-ui/03-RESEARCH.md — Pitfall 4 (dispatchNextPendingTracks must be synchronous non-async), Pitfall 5 (TrackItem must be class not struct), Pattern 5 (worker pool dispatch), Pattern 7 (cancel via terminate)
  </read_first>

  <action>
  Create SeratoDynamicAnalyzer/Models/TrackItem.swift. This is a new file in the existing Models/ directory. It must be a @MainActor class conforming to ObservableObject and Identifiable — NOT a struct (struct mutations cause full-array SwiftUI diffing across 50+ concurrent rows, per RESEARCH.md Pitfall 5). Do NOT use @Observable macro — it requires macOS 14+; project targets macOS 13.0. The file must define:
  - TrackStatus enum with raw String values: pending="Pending", analyzing="Analyzing", done="Done", failed="Failed"
  - TrackItem class with: let id = UUID(), let url: URL, computed var filename: String { url.lastPathComponent }, @Published var status: TrackStatus = .pending, @Published var progress: Double = 0.0, @Published var result: AnalysisResult? = nil, @Published var errorMessage: String? = nil, @Published var analysisDuration: TimeInterval? = nil, @Published var isExpanded: Bool = false, @Published var bpmMinOverride: Int? = nil, @Published var bpmMaxOverride: Int? = nil, init(url: URL)

  Create SeratoDynamicAnalyzer/ViewModels/BatchViewModel.swift. This is a new file in a new ViewModels/ subdirectory. It replaces AnalysisViewModel.swift as the root state owner. Follow the @MainActor class: ObservableObject pattern exactly from AnalysisViewModel.swift lines 6–7. The class must:
  - Be declared: @MainActor class BatchViewModel: ObservableObject
  - @Published properties: tracks: [TrackItem] = [], globalBpmMin: Int = 60, globalBpmMax: Int = 200, isDryRun: Bool = false, workerReadyCount: Int = 0, showSeratoAlert: Bool = false, isScanning: Bool = false
  - var skipSeratoCheck: Bool = false (non-published, same pattern as AnalysisViewModel)
  - private var workers: [PythonBridge] = [] (the dynamic pool, starts empty, grows to max 4)
  - private var assignments: [Int: TrackItem] = [:] (slotIndex → active TrackItem)
  - private var queuedURLs: Set<URL> = [] (deduplication; silently skip already-queued URLs)
  - Computed vars: completedCount: Int (filter .done or .failed), totalCount: Int (tracks.count), isAnalyzing: Bool (any .analyzing), canStartBatch: Bool (tracks not empty AND any .pending AND workerReadyCount > 0)
  - func startInitialWorker() async — creates one PythonBridge, appends to workers, calls startWorkerSlot(bridge:slotIndex:) for slot 0
  - func startWorkerSlot(bridge: PythonBridge, slotIndex: Int) async — calls bridge.startWorker(), then bridge.waitForReady(timeout: 30). On success: workerReadyCount += 1. On failure: set errorMessage-equivalent (since BatchViewModel has no single errorMessage, skip silently but print to stderr). Mirror AnalysisViewModel.startWorker() exactly.
  - func isSeratoRunning() -> Bool — copy verbatim from AnalysisViewModel.swift lines 40–43
  - func startBatch() — guard canStartBatch. If !isDryRun && !skipSeratoCheck && isSeratoRunning() { showSeratoAlert = true; return }. skipSeratoCheck = false. Call dispatchNextPendingTracks().
  - func dispatchNextPendingTracks() — MUST be synchronous (non-async, no await inside). This prevents actor reentrancy double-dispatch (RESEARCH.md Pitfall 4). Logic: (1) find pending tracks; (2) desired worker count = min(max(1, pendingTracks.count), 4); (3) if workers.count < desired, create and start new PythonBridge instances in a Task; (4) for each slot in workers.enumerated(), if assignments[slotIndex] == nil and there is a next pending track, assign it: set track.status = .analyzing, assignments[slotIndex] = track, capture startTime = Date(), launch a Task that calls bridge.analyzeStream(filePath: track.url.path, bpmMin: track.bpmMinOverride ?? globalBpmMin, bpmMax: track.bpmMaxOverride ?? globalBpmMax, dryRun: isDryRun) and iterates the event stream via handleWorkerEvent.
  - func handleWorkerEvent(_ event: WorkerEvent, trackItem: TrackItem, slotIndex: Int, startTime: Date) async — switch on event: .ready → break; .progress(_,pct) → trackItem.progress = Double(pct)/100.0; .result(r) → trackItem.result = r; trackItem.analysisDuration = Date().timeIntervalSince(startTime); trackItem.status = .done; assignments[slotIndex] = nil; dispatchNextPendingTracks(); .error(_,msg) → trackItem.errorMessage = msg; trackItem.analysisDuration = Date().timeIntervalSince(startTime); trackItem.status = .failed; assignments[slotIndex] = nil; dispatchNextPendingTracks()
  - func addDroppedURL(_ url: URL) async — check isDirectory via FileManager. If directory: set isScanning = true, run Task.detached(priority: .userInitiated) that calls scanFolder(_ url:) and returns [URL], then on MainActor: appendUniqueURLs(results); isScanning = false. If file: validate extension in ["mp3","aiff","aif","wav"] (case-insensitive), call appendUniqueURL(url).
  - private func scanFolder(_ url: URL) async -> [URL] — Task.detached with FileManager.default.enumerator(at: url, includingPropertiesForKeys: [.isRegularFileKey], options: [.skipsHiddenFiles, .skipsPackageDescendants]); collect URLs with pathExtension.lowercased() in ["mp3","aiff","aif","wav"]; append url.standardizedFileURL to results; return results.
  - private func appendUniqueURL(_ url: URL) — if !queuedURLs.contains(url): queuedURLs.insert(url); tracks.append(TrackItem(url: url))
  - private func appendUniqueURLs(_ urls: [URL]) — forEach call appendUniqueURL
  - func cancelTrack(_ item: TrackItem) — if item.status == .pending: remove from tracks array, remove from queuedURLs. If item.status == .analyzing: find slotIndex in assignments where assignments[slotIndex]?.id == item.id; call Task { await workers[slotIndex].terminate() }; item.errorMessage = "Cancelled by user"; item.status = .failed; assignments[slotIndex] = nil; dispatchNextPendingTracks()

  Modify SeratoDynamicAnalyzer/PythonBridge.swift — add one new method after analyzeStream, before the bundle path helpers section:
    func terminate() {
        process?.terminate()
    }
  This is actor-isolated (not nonisolated). The caller awaits it. The existing terminationHandler fires handleCrash(), setting isWorkerRunning = false, so the next analyzeStream call auto-restarts the worker (existing Phase 2 crash-recovery pattern is unchanged).

  All files must import Foundation. BatchViewModel must also import AppKit (for NSWorkspace in isSeratoRunning).
  </action>

  <verify>
    <automated>cd "/Users/andreasmrogenda/Claude Projekte/Serato Dynamic Analyzer" && DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild build -scheme SeratoDynamicAnalyzer -configuration Debug 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
  - TrackItem.swift exists at SeratoDynamicAnalyzer/Models/TrackItem.swift and declares `class TrackItem: ObservableObject, Identifiable` (not struct, not @Observable)
  - TrackStatus enum has exactly four cases: pending, analyzing, done, failed with String raw values "Pending", "Analyzing", "Done", "Failed"
  - BatchViewModel.swift exists at SeratoDynamicAnalyzer/ViewModels/BatchViewModel.swift and declares `@MainActor` and `class BatchViewModel: ObservableObject`
  - dispatchNextPendingTracks() has NO `async` keyword and NO `await` expressions inside it (synchronous only — prevents reentrancy)
  - BatchViewModel.workers is typed [PythonBridge] (array of actors, not optional, not a fixed-size array pre-filled with nil)
  - PythonBridge.swift contains `func terminate()` that calls `process?.terminate()`
  - BatchViewModel.addDroppedURL calls url.standardizedFileURL on every URL before appending (WR-06 — file reference URL fix)
  - scanFolder uses FileManager.default.enumerator options that include .skipsPackageDescendants
  - isSeratoRunning() checks bundleIdentifier == "com.serato.seratodj" (verbatim from AnalysisViewModel)
  - startBatch() checks isSeratoRunning() once for the whole batch (not per-track), sets showSeratoAlert if running and not dry run
  - xcodebuild BUILD SUCCEEDED (no compilation errors; new files must be added to Xcode project's Sources build phase)
  </acceptance_criteria>

  <done>TrackItem and BatchViewModel compile; PythonBridge has terminate(). xcodebuild BUILD SUCCEEDED.</done>
</task>

<task type="auto">
  <name>Task 2: BatchHeaderView, BatchQueueView, TrackRowView, ContentView refactor, delete AnalysisViewModel</name>
  <files>
    SeratoDynamicAnalyzer/Views/BatchHeaderView.swift,
    SeratoDynamicAnalyzer/Views/BatchQueueView.swift,
    SeratoDynamicAnalyzer/Views/TrackRowView.swift,
    SeratoDynamicAnalyzer/ContentView.swift,
    SeratoDynamicAnalyzer/AnalysisViewModel.swift
  </files>

  <read_first>
    - SeratoDynamicAnalyzer/ContentView.swift — current full file; understand what to keep (Serato alert block lines 90–98, .task line 100, imports lines 1–3) and what to remove (dropZone computed var, resultCard() function, single-track VStack body)
    - .planning/phases/03-batch-queue-drag-drop-ui/03-PATTERNS.md — BatchQueueView pattern (lines 203–298), BatchHeaderView pattern (lines 381–449), TrackRowView pattern (lines 301–378), ContentView modification instructions (lines 451–465)
    - .planning/phases/03-batch-queue-drag-drop-ui/03-UI-SPEC.md — Component 1 (Batch Header), Component 3 (Analyze All), Component 4 (Drop Zone), Component 5 (Queue List), Status Pill Contract, Copywriting Contract, Accessibility Contract, Window Sizing (640×480)
    - .planning/phases/03-batch-queue-drag-drop-ui/03-RESEARCH.md — Pattern 1 (NSItemProvider drop loop — must start loading synchronously, never deferred to Task.detached), Pitfall 2 (standardizedFileURL required), Anti-patterns (no .id() on ForEach children), State of the Art (animation on List insertion)
  </read_first>

  <action>
  Create SeratoDynamicAnalyzer/Views/BatchHeaderView.swift. This is a new file in a new Views/ subdirectory.

    struct BatchHeaderView: View with @ObservedObject var viewModel: BatchViewModel.

    Body: VStack(spacing: 0) containing:
    1. An HStack(.center, spacing: 16) with .padding(.horizontal, 24) and .padding(.vertical, 16) and .background(Color(nsColor: .controlBackgroundColor)):
       - Left: if viewModel.totalCount > 0, a VStack(alignment: .leading, spacing: 4) with Text("\(viewModel.completedCount) of \(viewModel.totalCount) tracks complete").font(.caption).foregroundColor(Color(nsColor: .secondaryLabelColor)) and ProgressView(value: Double(viewModel.completedCount), total: Double(viewModel.totalCount)).progressViewStyle(.linear).tint(.accentColor).animation(.linear(duration: 0.3), value: viewModel.completedCount). Frame this VStack with .frame(minWidth: 200).
       - Spacer()
       - BPM controls: HStack(spacing: 4) with Text("BPM").font(.caption).foregroundColor(Color(nsColor: .secondaryLabelColor)), Stepper("", value: $viewModel.globalBpmMin, in: 20...(viewModel.globalBpmMax - 1)).labelsHidden(), Text("\(viewModel.globalBpmMin)").font(.body).frame(minWidth: 36), Text("–"), Text("\(viewModel.globalBpmMax)").font(.body).frame(minWidth: 36), Stepper("", value: $viewModel.globalBpmMax, in: (viewModel.globalBpmMin + 1)...300).labelsHidden(). The entire BPM HStack is .disabled(viewModel.isAnalyzing).
       - Toggle("Dry Run", isOn: $viewModel.isDryRun).disabled(viewModel.isAnalyzing)
       - Button(viewModel.isAnalyzing ? "Analyzing…" : "Analyze All") { viewModel.startBatch() }.buttonStyle(.borderedProminent).disabled(!viewModel.canStartBatch)
    2. Worker warmup hint (below the HStack but above Divider): if viewModel.workerReadyCount == 0 && !viewModel.isAnalyzing, show Text("Starting analysis engine…").font(.caption).foregroundColor(.secondary).padding(.horizontal, 24).padding(.bottom, 4)
    3. Divider()

  Create SeratoDynamicAnalyzer/Views/BatchQueueView.swift. This is the primary content area view.

    struct BatchQueueView: View with @ObservedObject var viewModel: BatchViewModel, @State private var isTargeted: Bool = false, @State private var showFilePicker: Bool = false. Import SwiftUI, UniformTypeIdentifiers, AppKit.

    Body: VStack(spacing: 0):
    1. Queue List: if viewModel.tracks.isEmpty, show an empty state centered VStack with Text("No tracks in queue").font(.headline) and Text("Drop audio files or folders here to get started.").font(.caption).foregroundColor(.secondary). Otherwise, List { ForEach(viewModel.tracks) { item in TrackRowView(item: item, viewModel: viewModel) } }.listStyle(.plain).animation(.default, value: viewModel.tracks.count). Do NOT use .id() modifier on ForEach children.
    2. Drop zone strip (pinned at bottom): RoundedRectangle(cornerRadius: 8).strokeBorder(isTargeted ? Color.accentColor : Color.secondary, lineWidth: isTargeted ? 2 : 1).frame(height: 80).overlay(VStack(spacing: 4) { if viewModel.isScanning { HStack(spacing: 8) { ProgressView().scaleEffect(0.7); Text("Scanning folder…").font(.caption).foregroundColor(.secondary) } } else { Image(systemName: "arrow.down.doc").font(.system(size: 20)).foregroundColor(.secondary); Text("Drop files or folders").font(.caption).foregroundColor(.secondary) } }).animation(.easeInOut(duration: 0.15), value: isTargeted). Below the RoundedRectangle: Button("Choose Files…") { showFilePicker = true }.buttonStyle(.bordered).font(.caption). Wrap both in a VStack with .padding(.horizontal, 24).padding(.vertical, 12).background(Color(nsColor: .windowBackgroundColor)).
    3. Drop handler on the entire VStack: .onDrop(of: [UTType.fileURL], isTargeted: $isTargeted) { providers in for provider in providers { _ = provider.loadObject(ofClass: URL.self) { url, _ in guard let url = url else { return }; let resolved = url.standardizedFileURL; Task { @MainActor in await viewModel.addDroppedURL(resolved) } } }; return true }. CRITICAL: loadObject must start synchronously inside the loop before the closure returns. Never defer into Task.detached.
    4. NSOpenPanel for "Choose Files…": use .onChange(of: showFilePicker) or implement a helper func openFilePicker() that creates an NSOpenPanel with canChooseFiles = true, canChooseDirectories = true, allowsMultipleSelection = true, allowedContentTypes = [UTType.mp3, UTType(filenameExtension: "aiff") ?? .audio, UTType(filenameExtension: "wav") ?? .audio]. On runModal() == .OK: for each url in panel.urls, call Task { @MainActor in await viewModel.addDroppedURL(url.standardizedFileURL) }. Call openFilePicker() from the Button action instead of setting showFilePicker. (fileImporter cannot select directories — use NSOpenPanel per RESEARCH.md constraint.)
    5. Drop zone change indicator: when the window is targeted (isTargeted), the drop zone border changes to accentColor with lineWidth 2. This is already handled by the isTargeted binding on .onDrop.

  Create SeratoDynamicAnalyzer/Views/TrackRowView.swift. This is the per-row view. Import SwiftUI, AppKit.

    struct TrackRowView: View with @ObservedObject var item: TrackItem, @ObservedObject var viewModel: BatchViewModel.

    Body: An HStack(alignment: .center, spacing: 8) with .padding(.vertical, 8).padding(.horizontal, 16).frame(minHeight: 44):
    - StatusPillView(status: item.status) — inline struct defined below, width ~88pt fixed via .frame(width: 88)
    - Text(item.filename).font(.body).lineLimit(1).truncationMode(.middle).frame(maxWidth: .infinity, alignment: .leading)
    - Switch on item.status:
      - .analyzing: ProgressView().scaleEffect(0.8).tint(.accentColor).frame(width: 80)
      - .done: if let result = item.result { Text(String(format: "%.1f–%.1f", result.bpmMin, result.bpmMax)).font(.caption).frame(width: 64); Text("\(result.markerCount)").font(.caption).frame(width: 48) }
      - .failed: if let msg = item.errorMessage { Text(msg).font(.caption).foregroundColor(.red).lineLimit(1).frame(maxWidth: 160) }
      - .pending: Spacer().frame(width: 0) (no content in that column area)
    - Cancel button (Pending and Analyzing only): if item.status == .pending || item.status == .analyzing { CancelButton(item: item, viewModel: viewModel) }. For .done/.failed: Spacer().frame(width: 32).

    Define StatusPillView as a private struct with var status: TrackStatus. It renders an HStack(spacing: 4) with a leading icon and Text(status.rawValue).font(.caption). Pill colors per UI-SPEC Status Pill Contract: Pending = quaternaryLabelColor bg + secondaryLabelColor text + "clock" symbol; Analyzing = accentColor.opacity(0.15) bg + accentColor text + ProgressView().scaleEffect(0.6) spinner; Done = green.opacity(0.15) bg + green text + "checkmark.circle.fill"; Failed = red.opacity(0.15) bg + red text + "exclamationmark.circle.fill". Padding: 4pt vertical, 8pt horizontal. cornerRadius: 4. Use .font(.caption) for the label.

    Define CancelButton as a private struct with var item: TrackItem, @ObservedObject var viewModel: BatchViewModel, @State private var isHovered: Bool = false. Body: Button { viewModel.cancelTrack(item) } label: { Image(systemName: "xmark.circle.fill").font(.system(size: 16)).foregroundColor(isHovered ? Color(nsColor: .secondaryLabelColor) : Color(nsColor: .tertiaryLabelColor)) }.buttonStyle(.plain).frame(width: 32, height: 44).onHover { hovering in isHovered = hovering }.accessibilityLabel(item.status == .pending ? "Remove \(item.filename) from queue" : "Cancel analysis of \(item.filename)").

    Accessibility: StatusPillView has .accessibilityLabel("\(item.filename), status: \(item.status.rawValue)"). Batch progress ProgressView has .accessibilityValue("\(viewModel.completedCount) of \(viewModel.totalCount) tracks complete").

  Modify SeratoDynamicAnalyzer/ContentView.swift — surgical replacement:
  - Change line 6: @StateObject private var viewModel = AnalysisViewModel() → @StateObject private var viewModel = BatchViewModel()
  - Remove @State private var showFilePicker: Bool = false (moved to BatchQueueView)
  - Replace the entire body VStack with: VStack(spacing: 0) { BatchHeaderView(viewModel: viewModel); BatchQueueView(viewModel: viewModel) }
  - Change .frame(minWidth: 480, minHeight: 360) → .frame(minWidth: 640, minHeight: 480)
  - Change .task { await viewModel.startWorker() } → .task { await viewModel.startInitialWorker() }
  - Keep .alert("Serato DJ Pro is Running", …) block verbatim. Update the Continue Anyway button: viewModel.skipSeratoCheck = true; viewModel.startBatch(). (Previously called startAnalysis, now calls startBatch.)
  - Remove: dropZone computed property, resultCard() function, the fileImporter modifier, onDrop modifier (moved to BatchQueueView), isTargeted @State var. Keep: imports (lines 1–3), the struct declaration and body property.
  - Remove the entire onDrop from ContentView — BatchQueueView now owns the drop handler.

  Delete SeratoDynamicAnalyzer/AnalysisViewModel.swift — this file is fully superseded by BatchViewModel.swift. Remove it from the Xcode project's Sources build phase in project.pbxproj.

  Add all new files (TrackItem.swift, BatchViewModel.swift, BatchHeaderView.swift, BatchQueueView.swift, TrackRowView.swift) and the new directory entries to the Xcode project's Sources build phase. The Views/ and ViewModels/ subdirectories must be created on disk and the .swift files must be properly referenced in project.pbxproj (PBXBuildFile + PBXFileReference entries + Sources build phase membership).
  </action>

  <verify>
    <automated>cd "/Users/andreasmrogenda/Claude Projekte/Serato Dynamic Analyzer" && DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild build -scheme SeratoDynamicAnalyzer -configuration Debug 2>&1 | grep -E "(BUILD SUCCEEDED|BUILD FAILED|error:)"</automated>
  </verify>

  <acceptance_criteria>
  - BUILD SUCCEEDED with no compilation errors
  - AnalysisViewModel.swift is deleted from disk and removed from project.pbxproj Sources build phase
  - ContentView.swift contains `@StateObject private var viewModel = BatchViewModel()` (not AnalysisViewModel)
  - ContentView.swift contains `.frame(minWidth: 640, minHeight: 480)`
  - ContentView.swift contains `.task { await viewModel.startInitialWorker() }`
  - BatchQueueView.swift onDrop loop calls `provider.loadObject(ofClass: URL.self)` synchronously for each provider in a for loop (not just providers.first)
  - BatchQueueView.swift contains `.standardizedFileURL` on every URL received from drop handler
  - BatchQueueView.swift uses NSOpenPanel (not fileImporter) for the "Choose Files…" button, with canChooseDirectories = true
  - TrackRowView.swift uses @ObservedObject var item: TrackItem (class reference, not a binding to a struct)
  - TrackRowView.swift does NOT use @Observable import
  - StatusPillView uses Color(nsColor: .quaternaryLabelColor) for Pending background and Color.green.opacity(0.15) for Done background
  - No .id() modifier on ForEach children inside any List
  - The app launches and the window title/content shows the new batch queue layout (BatchHeaderView + queue list area), not the old single-track drop zone
  </acceptance_criteria>

  <done>
  BUILD SUCCEEDED. AnalysisViewModel.swift deleted. App launches showing batch queue layout: sticky header with "Analyze All" button, BPM steppers, Dry Run toggle, and a queue list area with compact drop strip at the bottom.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Full batch queue end-to-end slice:
  - Drop zone accepts multiple files and folders, populates Pending rows in the queue
  - Clicking "Analyze All" starts parallel analysis (1–4 workers)
  - Row status pills transition Pending → Analyzing → Done/Failed
  - Batch header counter "X of N tracks complete" updates live
  - Build is clean (BUILD SUCCEEDED, no AnalysisViewModel references)
  </what-built>
  <how-to-verify>
  1. Build and run: DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild build -scheme SeratoDynamicAnalyzer 2>&1 | tail -3
  2. Open the built app (find it under build/Debug or DerivedData). Window should be 640×480 minimum, showing the batch header at the top with "Analyze All" button and BPM steppers.
  3. Drop 3–5 MP3/AIFF/WAV files onto the window. Each file should appear as a row with a grey "Pending" status pill.
  4. Drop a folder containing audio files. The folder should recursively enumerate and add all MP3/AIFF/WAV files as Pending rows (non-audio files ignored).
  5. Click "Analyze All". Rows should transition to "Analyzing" with an inline spinner. The batch header counter should start incrementing.
  6. Wait for analysis to complete. Rows should show "Done" (green pill) or "Failed" (red pill). Counter should reach "N of N tracks complete".
  7. Drop a file a second time — it should be silently ignored (deduplication).
  8. Drop an unsupported file (e.g., a .txt file) — it should not appear in the queue.
  </how-to-verify>
  <resume-signal>Type "approved" if all behaviors work, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Finder → onDrop | User-controlled file paths enter via NSItemProvider. File reference URLs arrive here; must be standardized before use. |
| onDrop → BatchViewModel.addDroppedURL | Resolved URL passed to the VM; extension whitelist checked here before enqueueing. |
| BatchViewModel → PythonBridge (stdin JSON) | File path sent as a JSON field to the Python worker. JSON encoding prevents shell injection (no shell=True equivalent in Process). |
| FileManager.enumerator | Folder recursion on user-supplied path. Must skip package descendants to prevent traversal into .app bundles. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | BatchQueueView.onDrop — NSItemProvider URL | mitigate | Apply `url.standardizedFileURL` to every URL from `loadObject` before passing to VM. Converts file-reference URLs (file:///.file/id=…) to regular path URLs. Extension whitelist rejects non-audio files silently. |
| T-03-01-02 | Tampering | BatchViewModel.addDroppedURL — file extension check | mitigate | Guard `["mp3","aiff","aif","wav"].contains(ext)` before appending. Rejects .exe, .sh, .js, or any non-audio file silently. |
| T-03-01-03 | Tampering | BatchViewModel.scanFolder — recursive directory traversal | mitigate | FileManager.enumerator with `.skipsPackageDescendants` prevents descending into .app/.band/.bundle packages. Extension whitelist applied to every discovered file. |
| T-03-01-04 | Tampering | PythonBridge.analyzeStream — file path to Python stdin | accept | File path is serialized as a JSON string field, not concatenated into a shell command. Process.arguments is a [String] array — no shell interpolation. Same mitigation as Phase 2 (T-02-03 confirmed). |
| T-03-01-05 | Denial of Service | BatchViewModel worker pool — unbounded spawning | mitigate | Pool cap: `min(max(1, pendingTracks.count), 4)` — never more than 4 concurrent Python workers. Prevents CPU/RAM exhaustion (4 librosa processes × ~200 MB ≈ 800 MB max, acceptable on M-series Mac). |
| T-03-01-06 | Denial of Service | BatchViewModel.addDroppedURL — unlimited queue depth | accept | No hard limit on queue size; a DJ legitimately queues an entire library (thousands of tracks). Queue items are lightweight TrackItem objects (~1 KB each). Memory impact of the queue itself is negligible. Analysis rate is bounded by pool cap. |
| T-03-01-SC | Tampering | npm/pip/cargo installs | accept | No new external packages in Phase 3. All components are macOS system frameworks + existing project stack. Package legitimacy gate not applicable. |
</threat_model>

<verification>
After both tasks complete and human-verify passes:
1. `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild build -scheme SeratoDynamicAnalyzer 2>&1 | grep "BUILD SUCCEEDED"` — must pass
2. `grep -r "AnalysisViewModel" "/Users/andreasmrogenda/Claude Projekte/Serato Dynamic Analyzer/SeratoDynamicAnalyzer/" --include="*.swift"` — must return empty (no lingering references)
3. `grep -r "@Observable" "/Users/andreasmrogenda/Claude Projekte/Serato Dynamic Analyzer/SeratoDynamicAnalyzer/" --include="*.swift"` — must return empty (macOS 13 constraint)
4. `grep -r "\.id(" "/Users/andreasmrogenda/Claude Projekte/Serato Dynamic Analyzer/SeratoDynamicAnalyzer/Views/" --include="*.swift"` — must return empty (no .id() on ForEach children)
5. Manual smoke: drop 3 MP3s → Pending rows appear → "Analyze All" starts → rows transition to Done → counter reaches "3 of 3 tracks complete"
</verification>

<success_criteria>
- Building succeeds with no errors
- AnalysisViewModel.swift is gone
- Single-file drop → Pending row appears immediately (INPUT-01)
- Folder drop → all MP3/AIFF/WAV files recursively discovered and added as Pending rows (INPUT-02)
- "Analyze All" starts up to 4 parallel workers (BATCH-01)
- Batch header counter increments live as tracks complete (BATCH-02)
- Each row shows exactly one of: Pending pill / Analyzing pill + spinner / Done pill / Failed pill (UI-01)
</success_criteria>

<output>
Create `.planning/phases/03-batch-queue-drag-drop-ui/03-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | File | Kind |
|--------|------|------|
| TrackStatus | SeratoDynamicAnalyzer/Models/TrackItem.swift | enum |
| TrackItem | SeratoDynamicAnalyzer/Models/TrackItem.swift | class (ObservableObject) |
| BatchViewModel | SeratoDynamicAnalyzer/ViewModels/BatchViewModel.swift | class (ObservableObject) |
| BatchHeaderView | SeratoDynamicAnalyzer/Views/BatchHeaderView.swift | SwiftUI View struct |
| BatchQueueView | SeratoDynamicAnalyzer/Views/BatchQueueView.swift | SwiftUI View struct |
| TrackRowView | SeratoDynamicAnalyzer/Views/TrackRowView.swift | SwiftUI View struct |
| StatusPillView | SeratoDynamicAnalyzer/Views/TrackRowView.swift | private SwiftUI View struct |
| CancelButton | SeratoDynamicAnalyzer/Views/TrackRowView.swift | private SwiftUI View struct |
| PythonBridge.terminate() | SeratoDynamicAnalyzer/PythonBridge.swift | actor method |
| ContentView (modified) | SeratoDynamicAnalyzer/ContentView.swift | SwiftUI View struct (modified) |
| AnalysisViewModel.swift | SeratoDynamicAnalyzer/AnalysisViewModel.swift | DELETED |
</output>
