---
phase: 04-serato-library-browser
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - SeratoDynamicAnalyzer/DesignSystem.swift
  - SeratoDynamicAnalyzer/Views/LibraryView.swift
  - SeratoDynamicAnalyzer/Views/CrateSidebarView.swift
  - SeratoDynamicAnalyzer/Views/LibraryTrackTableView.swift
  - SeratoDynamicAnalyzer/Views/LibraryTrackRow.swift
  - SeratoDynamicAnalyzer/Views/LibraryToolbarView.swift
  - SeratoDynamicAnalyzer/Views/LibraryEmptyStateView.swift
  - SeratoDynamicAnalyzer/ContentView.swift
  - SeratoDynamicAnalyzer.xcodeproj/project.pbxproj
  - Makefile
autonomous: false

requirements: [LIB-01, LIB-02, LIB-03]

must_haves:
  truths:
    - "The app shows a TabView with Library (book.fill) and Queue (list.bullet) tabs; the Queue tab contains the unchanged BatchHeaderView + BatchQueueView"
    - "The Library tab is an HSplitView: crate sidebar (OutlineGroup, nested crates) left, track table right"
    - "Each library track row shows a colored beatgrid dot: gray=none, kdWarning(yellow)=serato, kdPrimary(cyan)=tool"
    - "Multi-selecting tracks and clicking ANALYZE SELECTED appends them to the queue and auto-switches to the Queue tab"
    - "When ~/Music/_Serato_/ is absent the Library tab shows the empty-state instructions, no file picker"
    - "Serato DJ Pro accepts a GEOB BeatGrid written with version byte[1]=0x01 and displays the grid correctly (human-verified)"
  artifacts:
    - path: "SeratoDynamicAnalyzer/DesignSystem.swift"
      provides: "kdWarning amber token"
      contains: "kdWarning"
    - path: "SeratoDynamicAnalyzer/Views/LibraryView.swift"
      provides: "HSplitView library layout"
      contains: "HSplitView"
    - path: "SeratoDynamicAnalyzer/ContentView.swift"
      provides: "TabView restructure"
      contains: "TabView"
  key_links:
    - from: "CrateSidebarView OutlineGroup selection"
      to: "LibraryViewModel.selectCrate"
      via: "onChange(of: selectedCrateID) → flattenedFind → selectCrate"
      pattern: "selectCrate"
    - from: "LibraryToolbarView ANALYZE SELECTED button"
      to: "LibraryViewModel.analyzeSelected"
      via: "button action with batchViewModel + selectedTab binding"
      pattern: "analyzeSelected"
    - from: "LibraryTrackRow dot"
      to: "BeatgridSource"
      via: "dotColor switch on track.beatgridSource"
      pattern: "kdWarning"
---

<objective>
Build the complete user-facing Library Browser and wire it into the app shell. Adds the missing `kdWarning` design token, the six new SwiftUI views (LibraryView HSplitView, CrateSidebarView OutlineGroup, LibraryTrackTableView, LibraryTrackRow with beatgrid dot, LibraryToolbarView CTA, LibraryEmptyStateView), the ContentView TabView restructure, and the Makefile change that bundles `library.py` alongside `analyze.py`.

This is the final vertical slice: after this plan a DJ opens the app, lands on the Library tab, browses the nested crate hierarchy, sees per-track beatgrid dots, multi-selects tracks, clicks ANALYZE SELECTED, and lands on the Queue tab with the tracks Pending. A blocking human-verify gate confirms Serato DJ Pro accepts the new version byte (D-07 risk A1).

Purpose: Deliver LIB-01/LIB-02/LIB-03 end-to-end and de-risk the version-byte differentiator before phase completion.
Output: 6 new view files, kdWarning token, restructured ContentView, Makefile bundling, pbxproj membership, and a Serato acceptance checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-serato-library-browser/04-UI-SPEC.md
@.planning/phases/04-serato-library-browser/04-PATTERNS.md
@.planning/phases/04-serato-library-browser/04-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan (new — exclude from drift verification):
- `Color.kdWarning` static token in DesignSystem.swift
- SwiftUI views: `LibraryView`, `CrateSidebarView`, `LibraryTrackTableView`, `LibraryTrackRow`, `LibraryToolbarView`, `LibraryEmptyStateView`
- ContentView gains `@StateObject libraryViewModel`, `@State selectedTab: AppTab`, and a TabView body (AppTab enum already declared in LibraryViewModel.swift from Plan 02 — reuse, do NOT redeclare)
- Makefile: `library.py` copied into `$(SCRIPTS_DIR)` in the bundle-python target
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: kdWarning token + Library views (LIB-01, LIB-02, LIB-03)</name>
  <files>SeratoDynamicAnalyzer/DesignSystem.swift, SeratoDynamicAnalyzer/Views/LibraryView.swift, SeratoDynamicAnalyzer/Views/CrateSidebarView.swift, SeratoDynamicAnalyzer/Views/LibraryTrackTableView.swift, SeratoDynamicAnalyzer/Views/LibraryTrackRow.swift, SeratoDynamicAnalyzer/Views/LibraryToolbarView.swift, SeratoDynamicAnalyzer/Views/LibraryEmptyStateView.swift, SeratoDynamicAnalyzer.xcodeproj/project.pbxproj</files>
  <read_first>
    - SeratoDynamicAnalyzer/DesignSystem.swift (FULL — first extension Color block where kdWarning is added; existing tokens kdPrimary/kdSurface/kdOnSurface/kdMuted/kdOutline/kdBg)
    - SeratoDynamicAnalyzer/Views/TrackRowView.swift (HStack row layout, 3pt edge bar, hover background, color tokens analog)
    - SeratoDynamicAnalyzer/Views/BatchQueueView.swift (List + scrollContentBackground + listStyle pattern; empty-state pattern)
    - SeratoDynamicAnalyzer/ViewModels/LibraryViewModel.swift (the @Published properties + selectCrate/analyzeSelected the views bind to; AppTab enum location)
    - SeratoDynamicAnalyzer/Models/SeratoCrate.swift (flattenedFind, isSelectable, children)
    - SeratoDynamicAnalyzer.xcodeproj/project.pbxproj (Views PBXGroup — register all 6 new view files)
    - .planning/phases/04-serato-library-browser/04-UI-SPEC.md (FULL — every layout/spacing/color/copy spec for these views is locked here)
    - .planning/phases/04-serato-library-browser/04-PATTERNS.md (CrateSidebarView/LibraryTrackRow/LibraryView exact shapes)
  </read_first>
  <action>
    D-08 COLOR DECISION (confirmed by user): D-08 specifies "green dot = this-tool-analyzed". The existing `kdPrimary` token (0x00F0FF cyan) satisfies D-08's green/this-tool requirement — confirmed by user; no new token needed. Use kdPrimary for the .tool dot. (This was previously flagged as a cyan-vs-green mismatch; the user has accepted kdPrimary cyan as the "green" this-tool indicator. Do NOT introduce a separate green token.)

    Add to DesignSystem.swift first `extension Color` block (after kdOutline): `static let kdWarning = Color(kdHex: 0xF5C518)  // amber yellow — Serato beatgrid dot`. Create the six view files per UI-SPEC; all use Kinetic Dark tokens only (never system colors except `Color.gray.opacity(0.35)` for the no-beatgrid dot per the locked color map). LibraryView.swift: `struct LibraryView: View` with `@ObservedObject var libraryViewModel`, `@ObservedObject var batchViewModel`, `@Binding var selectedTab: AppTab`; body = HSplitView { CrateSidebarView(viewModel: libraryViewModel).frame(minWidth:180, idealWidth:220, maxWidth:320); right panel }.background(Color.kdBg). Right panel = VStack(spacing:0){ libraryContent; LibraryToolbarView(...) }; libraryContent is @ViewBuilder showing LibraryEmptyStateView when !seratoLibraryExists else the column header row + LibraryTrackTableView. CrateSidebarView.swift: List(selection: $viewModel.selectedCrateID){ OutlineGroup(viewModel.crateTree, id:\.id, children:\.children){ crate in Label(crate.name, systemImage: crate.id == viewModel.selectedCrateID ? "folder.fill" : "folder").font(.kdBody).foregroundStyle(crate.isSelectable ? Color.kdOnSurface : Color.kdMuted).tag(crate.id) } }.listStyle(.sidebar).scrollContentBackground(.hidden).background(Color.kdSurface).onChange(of: viewModel.selectedCrateID){ id in if let id, let c = viewModel.crateTree.flattenedFind(id:id) { viewModel.selectCrate(c) } }; when isLoadingCrates show a centered ProgressView().tint(Color.kdPrimary) replacing the tree. LibraryTrackTableView.swift: a pinned column-header HStack ("FILENAME"/"DURATION"/"BPM" in .kdLabel/.kdMuted per UI-SPEC widths 56/72), then List(viewModel.trackList, id:\.id, selection: $viewModel.selectedTrackIDs){ track in LibraryTrackRow(track: track).tag(track.id) }.listStyle(.plain).scrollContentBackground(.hidden).background(Color.kdBg); show ProgressView + "Loading tracks..." when isLoadingTracks; show "Select a crate to view tracks" when selectedCrateID == nil. LibraryTrackRow.swift: HStack with 3pt selection edge bar, 8pt beatgrid Circle (dotColor: .tool→kdPrimary (the confirmed this-tool/D-08 green per the decision note above), .serato→kdWarning, .none→Color.gray.opacity(0.35)), filename (.kdBody/.kdOnSurface, lineLimit 1, truncationMode .middle), duration (.kdMono/.kdMuted, MM:SS or "—:——" when nil), frame(minHeight:44). LibraryToolbarView.swift: 44pt bar, "{N} selected" label (hidden when empty), Button("ANALYZE SELECTED") kdPrimary fill / kdBg fg when enabled, kdOutline/kdMuted when disabled, .disabled(selectedTrackIDs.isEmpty), action calls libraryViewModel.analyzeSelected(batchViewModel:, selectedTab:). LibraryEmptyStateView.swift: centered "No Serato Library Found" heading + "Open Serato DJ Pro once to create your library at ~/Music/_Serato_/." body, kdOnSurface/kdMuted, no file picker (D-06). Register all 6 files in project.pbxproj Views group with new unique hex IDs.
  </action>
  <verify>
    <automated>cd "$PWD" && DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -scheme SeratoDynamicAnalyzer -quiet build 2>&1 | grep -E "BUILD SUCCEEDED|BUILD FAILED|error:" | head -5</automated>
    <automated>cd "$PWD/python" && ./venv/bin/pytest tests/ -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "kdWarning" SeratoDynamicAnalyzer/DesignSystem.swift` >= 1
    - LibraryView.swift contains `HSplitView`; CrateSidebarView.swift contains `OutlineGroup` and `.onChange(of: viewModel.selectedCrateID`
    - LibraryTrackRow.swift dotColor maps .tool→kdPrimary, .serato→kdWarning, .none→Color.gray
    - LibraryToolbarView.swift contains `.disabled(` on selectedTrackIDs.isEmpty and calls analyzeSelected
    - LibraryEmptyStateView.swift contains the copy "No Serato Library Found"
    - All 6 view files registered in project.pbxproj (each appears as PBXBuildFile + PBXFileReference)
    - BUILD SUCCEEDED
    - Python suite still green: `cd python && ./venv/bin/pytest tests/ -q` exits 0 (view-layer changes do not touch the Python contract)
  </acceptance_criteria>
  <done>kdWarning exists; all six views compile and bind to LibraryViewModel; the .tool dot uses kdPrimary per the confirmed D-08 decision; build is green and the Python suite stays green.</done>
</task>

<task type="auto">
  <name>Task 2: ContentView TabView restructure + Makefile library.py bundling</name>
  <files>SeratoDynamicAnalyzer/ContentView.swift, Makefile</files>
  <read_first>
    - SeratoDynamicAnalyzer/ContentView.swift (FULL current 27 lines — the .alert, .task, .frame modifiers that must carry forward unchanged)
    - SeratoDynamicAnalyzer/ViewModels/LibraryViewModel.swift (AppTab enum already declared here — ContentView reuses it, must NOT redeclare)
    - SeratoDynamicAnalyzer/Views/LibraryView.swift (init signature: libraryViewModel, batchViewModel, selectedTab binding)
    - Makefile lines 65-76 (bundle-python target — the `cp python/analyze.py "$(SCRIPTS_DIR)/"` line to duplicate for library.py)
    - .planning/phases/04-serato-library-browser/04-UI-SPEC.md section 1 (ContentView TabView structure; default tab .library)
    - .planning/phases/04-serato-library-browser/04-PATTERNS.md (ContentView RESTRUCTURE block; rename viewModel → batchViewModel in the alert binding)
  </read_first>
  <action>
    Restructure ContentView.swift: rename the existing `@StateObject private var viewModel = BatchViewModel()` to `@StateObject private var batchViewModel = BatchViewModel()`; add `@StateObject private var libraryViewModel = LibraryViewModel()` and `@State private var selectedTab: AppTab = .library`. Body: VStack(spacing:0){ AppHeaderView(); TabView(selection: $selectedTab){ LibraryView(libraryViewModel: libraryViewModel, batchViewModel: batchViewModel, selectedTab: $selectedTab).tabItem{ Label("Library", systemImage:"book.fill") }.tag(AppTab.library); VStack(spacing:0){ BatchHeaderView(viewModel: batchViewModel); BatchQueueView(viewModel: batchViewModel) }.tabItem{ Label("Queue", systemImage:"list.bullet") }.tag(AppTab.queue) } }. Keep the .background(Color.kdBg), .frame(minWidth:700, idealWidth:820, minHeight:480), and the .alert("Serato DJ Pro is Running"...) modifier — but update its binding and button bodies from `viewModel` to `batchViewModel` (identical logic). Keep `.task { await batchViewModel.startInitialWorker() }` and ADD `.task { libraryViewModel.loadCrateTree() }`. Do NOT redeclare AppTab (it lives in LibraryViewModel.swift). In Makefile bundle-python target, after the `cp python/analyze.py "$(SCRIPTS_DIR)/"` line, add `cp python/library.py "$(SCRIPTS_DIR)/"` so the worker can `from library import ...` at runtime inside the bundle. Update the bundle-python echo/comment to mention library.py.
  </action>
  <verify>
    <automated>cd "$PWD" && DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -scheme SeratoDynamicAnalyzer -quiet build 2>&1 | grep -E "BUILD SUCCEEDED|BUILD FAILED|error:" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - ContentView.swift contains `TabView(selection: $selectedTab)`, `Label("Library", systemImage: "book.fill")`, `Label("Queue", systemImage: "list.bullet")`
    - ContentView.swift contains `@StateObject private var libraryViewModel` and `.task { libraryViewModel.loadCrateTree() }`
    - ContentView.swift does NOT contain a second `enum AppTab` declaration (grep -c "enum AppTab" in ContentView.swift == 0)
    - The Serato .alert binding references `batchViewModel.showSeratoAlert` (renamed from viewModel)
    - `grep -c "library.py" Makefile` >= 1 (bundling line present)
    - BUILD SUCCEEDED
  </acceptance_criteria>
  <done>App shell is a TabView with Library + Queue tabs; library worker loads crates on launch; library.py is bundled; build is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Serato version-byte acceptance + Library browser smoke test (D-07 risk A1)</name>
  <what-built>
    The full Serato Library Browser: TabView shell, crate sidebar, per-track beatgrid dots, multi-select → ANALYZE SELECTED → Queue auto-switch. analyze.py now writes BeatGrid GEOB tags with version byte[1]=0x01 (the "this tool" differentiator). RESEARCH assumption A1 — that Serato DJ Pro accepts byte[1]=0x01 — is UNTESTED against real Serato and must be confirmed before the phase ships.
  </what-built>
  <how-to-verify>
    1. Build + bundle + run the app:
       `make bundle-python && DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -scheme SeratoDynamicAnalyzer build` then launch the built .app (or run from Xcode).
    2. VERSION BYTE ACCEPTANCE (the critical gate):
       - Pick one disposable test track (keep a copy — the .serato-backup is created automatically).
       - In the Queue tab, drop the track and Analyze it (this writes a 0x01 0x01 BeatGrid).
       - Open that exact track in Serato DJ Pro. Confirm: the beatgrid displays, is aligned, and Serato shows NO error / does not blank the grid.
       - If the grid displays correctly → version byte accepted → type "approved".
       - If Serato rejects/blanks the grid → type "REJECTED" and describe; the fallback is the GEOB:Serato Analysis absence heuristic (UI-SPEC Wave 0 prerequisite #2 / RESEARCH Open Question 1) — a follow-up gap plan will swap read_beatgrid_source to the heuristic.
    3. LIBRARY BROWSER SMOKE (LIB-01/LIB-02/LIB-03):
       - Library tab: confirm the crate sidebar shows your nested crate hierarchy (expand a %% nested crate).
       - Select a crate: confirm the track table lists filename + duration, and each row shows a dot — gray (none), yellow (Serato), or cyan (this tool). The track you just analyzed in step 2 should now show a CYAN dot (kdPrimary = this-tool/D-08 green).
       - Cmd/Shift-click to multi-select 2+ tracks; confirm "{N} selected" appears and ANALYZE SELECTED enables.
       - Click ANALYZE SELECTED; confirm the app switches to the Queue tab with those tracks Pending.
    4. EMPTY STATE (optional, only if you can temporarily rename ~/Music/_Serato_): confirm "No Serato Library Found" instructions show with no file picker.
  </how-to-verify>
  <action>
    Human-only verification gate — no autonomous code change. The executor pauses and the user performs the steps in <how-to-verify>: build + bundle + launch the app, analyze one track to write a 0x01 0x01 BeatGrid, open it in Serato DJ Pro to confirm the grid is accepted (the untested D-07 assumption A1), then smoke-test the library browse → multi-select → ANALYZE SELECTED → Queue auto-switch flow. If Serato REJECTS the version byte, do NOT mark the phase complete: record the rejection in the SUMMARY and trigger a follow-up gap plan that switches read_beatgrid_source() in library.py to the GEOB:Serato Analysis absence heuristic (RESEARCH Open Question 1 fallback), keeping pack_beatgrid at 0x01 0x00 if needed. This checkpoint is never auto-approvable — wait for an explicit human resume signal.
  </action>
  <verify>
    <human-check>User confirms Serato DJ Pro displays the 0x01 0x01 beatgrid correctly AND the browse → select → ANALYZE SELECTED → Queue flow works end-to-end.</human-check>
  </verify>
  <done>User types "approved": Serato accepts the version byte and the full library browser flow is verified. Any "REJECTED"/"issues" response blocks phase completion and routes to a gap plan.</done>
  <resume-signal>Type "approved" if the version byte is accepted AND the browser flow works; type "REJECTED: <detail>" or "issues: <detail>" otherwise.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GEOB version byte → Serato DJ Pro | this tool writes byte[1]=0x01; Serato reads it — untested acceptance (A1) |
| User selection → batch queue | library track URLs cross into the write-capable analysis pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-VB | Tampering | 0x01 0x01 BeatGrid GEOB | mitigate | Blocking human-verify (Task 3) confirms Serato accepts the tag before phase completion; fallback heuristic documented if rejected (A1) |
| T-04-WRITE | Tampering | ANALYZE SELECTED → write | accept | Reuses Phase 2 safety: automatic .serato-backup + Serato-running alert before any write; library flow only enqueues Pending tracks (no write until Analyze All) |
| T-04-UISTDOUT | Information Disclosure | view-layer print() | mitigate | Views use Kinetic Dark tokens and no print(); diagnostics already routed to stderr in the ViewModels |
</threat_model>

<verification>
- `make bundle-python` copies both analyze.py and library.py into Resources/scripts
- `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -scheme SeratoDynamicAnalyzer build` → BUILD SUCCEEDED
- `cd python && ./venv/bin/pytest tests/ -q` exits 0 (Plan 01 suite remains green through the UI build)
- Human gate (Task 3): Serato accepts the 0x01 0x01 BeatGrid AND the browse→select→analyze→queue flow works end-to-end
</verification>

<success_criteria>
- LIB-01: crate sidebar renders the nested hierarchy from ~/Music/_Serato_/
- LIB-02: track table shows filename/duration/BPM; multi-select + ANALYZE SELECTED enqueues and switches to Queue
- LIB-03: per-track dot distinguishes none/serato/tool (kdPrimary cyan = this-tool/D-08 green, confirmed); version byte confirmed accepted by Serato
- App builds, bundles library.py, and the full flow is human-verified
</success_criteria>

<output>
Create `.planning/phases/04-serato-library-browser/04-03-SUMMARY.md` when done
</output>
