---
phase: 05-format-expansion-distribution
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - Makefile
  - README.md
autonomous: true
requirements:
  - FMT-04
  - FMT-05
must_haves:
  truths:
    - "Running `git tag vX.Y.Z && make publish` creates a GitHub Release with the DMG attached"
    - "The README contains a Download section with GitHub Releases link and Gatekeeper bypass instructions"
    - "make publish depends on make release (build + sign + dmg already present)"
    - "The tag is pushed to the public origin remote before gh release create runs"
    - "make publish is listed in .PHONY"
    - "make publish runs make release first (which rebuilds the entire .app bundle from current source including M4A/MP4 support) before calling gh release create — the published DMG is always built from the current HEAD"
  artifacts:
    - path: "Makefile"
      provides: "publish target depending on release, pushing tag via scripts/push-all.sh, calling gh release create with --generate-notes"
    - path: "README.md"
      provides: "Installation section with GitHub Releases download link and both Gatekeeper bypass options"
  key_links:
    - from: "Makefile publish target"
      to: "scripts/push-all.sh"
      via: "shell call inside publish recipe"
      pattern: "push-all.sh"
    - from: "Makefile publish target"
      to: "gh release create"
      via: "gh CLI call with --generate-notes"
      pattern: "gh release create"
---

## Phase Goal

**As a** DJ, **I want to** download the app from a GitHub Releases page, **so that** I can install it on my Mac without needing developer tools or Python.

<objective>
Deliver the distribution vertical slice: add `make publish` target to Makefile (reads version from git tag, pushes tag to origin, calls `gh release create` with DMG upload and auto-generated release notes), and add an Installation section to README.md with the GitHub Releases download link and both Gatekeeper bypass options.

Purpose: D-02 and D-03 — manual release workflow where developer runs `git tag vX.Y.Z && make publish` to produce a downloadable, ad-hoc-signed DMG on GitHub Releases. This is the distribution half of phase success criteria #2 (adjusted per D-01: ad-hoc signing with README Gatekeeper bypass instructions).

Output: Makefile has `publish` target, README has Installation section, `make publish` completes without error in a dry-run check.
</objective>

<execution_context>
@/Users/andreasmrogenda/.claude/gsd-core/workflows/execute-plan.md
@/Users/andreasmrogenda/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-format-expansion-distribution/05-CONTEXT.md
@.planning/phases/05-format-expansion-distribution/05-RESEARCH.md
@.planning/phases/05-format-expansion-distribution/05-PATTERNS.md
@.planning/phases/05-format-expansion-distribution/05-01-SUMMARY.md
@.planning/phases/05-format-expansion-distribution/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Makefile `publish` target (D-02, D-03)</name>
  <files>Makefile</files>

  <read_first>
    - Makefile — full file: understand line 37 (.PHONY declaration), lines 40–42 (release target syntax), lines 115–123 (dmg target as the last target before clean), and the APP_NAME variable. Note the Makefile tab indentation convention (must use hard tabs, not spaces).
    - .planning/phases/05-format-expansion-distribution/05-PATTERNS.md — "Makefile — publish target" section: exact publish target code, .PHONY extension, APP_NAME variable usage
    - .planning/phases/05-format-expansion-distribution/05-RESEARCH.md — "Pattern 4: make publish Makefile target" and "Pitfall 5: gh release create Without Pushing Tag First" — must push tag via scripts/push-all.sh before gh release create
    - scripts/push-all.sh — read to understand how it accepts arguments (tag or branch) to push to both remotes
  </read_first>

  <action>
  Update line 37: add `publish` to the .PHONY declaration: `.PHONY: release deps bundle-python bundle-swift sign dmg clean distclean publish`

  Add the publish target after the dmg target (after the existing @echo "dmg: ..." line, before the clean target). The recipe must:
  1. Capture VERSION from `git describe --tags --abbrev=0`
  2. Echo "Publishing release $$VERSION..."
  3. Call `./scripts/push-all.sh "$$VERSION"` to push the tag to both remotes (dual-remote setup per MEMORY.md)
  4. Call `gh release create "$$VERSION"` with the DMG asset aliased as `"build/$(APP_NAME).dmg#$(APP_NAME)-$$VERSION.dmg"`, flags `--title "Serato Dynamic Analyzer $$VERSION"`, `--generate-notes`, `--repo shatakaan/serato-dynamic-analyzer`
  5. Echo "publish: GitHub Release published"

  The target dependency line is: `publish: release`

  All recipe lines must use hard tab indentation (not spaces) — match existing Makefile style. Use `$$` (double dollar) for shell variables in Makefile recipes.

  Do NOT add --draft flag (per RESEARCH.md Discretion recommendation: publish directly, developer is present).
  Do NOT add --verify-tag (scripts/push-all.sh push ensures tag exists on remote before gh create runs).
  </action>

  <verify>
    <automated>grep -E '^\.PHONY' Makefile | grep -c 'publish' && make -n publish 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `grep -E '^\.PHONY' Makefile | grep 'publish'` matches (publish in .PHONY)
    - `grep -n '^publish:' Makefile` returns one match with `release` as dependency
    - `grep 'gh release create' Makefile` returns one match with '--generate-notes' and '--repo shatakaan/serato-dynamic-analyzer'
    - `grep 'push-all.sh' Makefile` returns one match (tag push before gh create)
    - `make -n publish 2>&1 | head -10` shows dry-run of: make release steps, push-all.sh call, gh release create call (no syntax errors)
    - `grep -P '^\t' Makefile | grep 'gh release' | wc -l` returns 1 (tab-indented recipe line)
  </acceptance_criteria>

  <done>Makefile has publish target in .PHONY, correct recipe with push-all.sh + gh release create, make -n publish runs without Makefile parse errors</done>
</task>

<task type="auto">
  <name>Task 2: README.md Installation section with Gatekeeper bypass (D-01)</name>
  <files>README.md</files>

  <read_first>
    - README.md — full file: understand current structure (headings, sections, tone) so the new Installation section fits the existing style and is placed logically (after any intro/overview, before any usage/contributing sections)
    - .planning/phases/05-format-expansion-distribution/05-RESEARCH.md — "Discretion recommendation for README Gatekeeper bypass" section: exact two-option Installation section text with Option A (right-click) and Option B (xattr -d command)
  </read_first>

  <action>
  Add an "## Installation" or "## Download & Install" section to README.md. Position: near the top of the README, after any project description/overview, before any "Usage" or "Development" sections.

  The section must contain:
  1. A Download subsection pointing to the GitHub Releases page: "Download the latest `SeratoDynamicAnalyzer-vX.Y.Z.dmg` from the [Releases page](https://github.com/shatakaan/serato-dynamic-analyzer/releases)."
  2. Install instructions: "Open the DMG and drag `SeratoDynamicAnalyzer.app` to your Applications folder."
  3. A Gatekeeper bypass subsection titled "First Launch — Gatekeeper Bypass Required" explaining this app is ad-hoc signed (not notarized by Apple) with BOTH bypass options:
     - Option A: Right-click the app in Finder -> "Open" -> click "Open" in the security dialog
     - Option B: Terminal command `xattr -d com.apple.quarantine /Applications/SeratoDynamicAnalyzer.app`
  4. A note that the app is Apple Silicon native and runs on Intel Macs via Rosetta 2 (macOS 13+).

  Match the tone and heading style of the existing README. Do not add emojis.
  </action>

  <verify>
    <automated>grep -c 'com.apple.quarantine\|xattr' README.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'com.apple.quarantine' README.md` returns at least 1
    - `grep -c 'right-click\|Right-click\|right click' README.md` returns at least 1 (Option A)
    - `grep -c 'Releases\|releases' README.md` returns at least 1 (GitHub Releases link)
    - `grep -c 'shatakaan/serato-dynamic-analyzer' README.md` returns at least 1 (repo URL)
    - `grep -c 'Rosetta' README.md` returns at least 1 (architecture note)
    - `grep '## ' README.md | grep -iE 'install|download'` returns at least 1 match (section heading present)
  </acceptance_criteria>

  <done>README.md has Installation section with GitHub Releases link, DMG install steps, and both Gatekeeper bypass options</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| make publish → gh CLI | Makefile recipe invokes gh CLI with git-tagged version; no user-supplied input |
| Downloaded DMG → user's Mac | Ad-hoc-signed app crosses Gatekeeper boundary; user must bypass quarantine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Spoofing | Ad-hoc signing + Gatekeeper | accept | Ad-hoc signing per D-01 (no Developer Account). README provides both bypass options. Users are DJs/developers who understand one-time override. Risk accepted — documented in CONTEXT.md. |
| T-05-04 | Tampering | make publish git tag injection | accept | VERSION is read from `git describe --tags --abbrev=0`; this is a developer-only target (not CI-exposed); tag must pre-exist as a local git tag created by the developer — no external input |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan; Makefile only adds a shell target |
</threat_model>

<verification>
1. Makefile parse check: `make -n publish 2>&1 | head -20` shows dry-run steps without "missing separator" or Makefile errors
2. .PHONY includes publish: `grep -E '^\.PHONY' Makefile | grep publish`
3. README contains quarantine command: `grep 'com.apple.quarantine' README.md`
4. README contains Releases link: `grep 'shatakaan/serato-dynamic-analyzer/releases' README.md`
5. Duplicate check — no existing publish target: `grep -n '^publish:' Makefile | wc -l` returns 1
</verification>

<success_criteria>
- D-02/D-03: `git tag v1.0.0 && make publish` produces a GitHub Release with DMG attached (one-command workflow confirmed by make -n dry-run)
- D-01: README Installation section explains both Gatekeeper bypass options (right-click and xattr)
- Phase 5 success criteria #2 (adjusted per D-01): GitHub Releases contains a downloadable, ad-hoc-signed DMG with README installation section explaining Gatekeeper bypass
- No regressions: existing Makefile targets (release, sign, dmg, clean) unaffected
</success_criteria>

## Artifacts This Phase Produces

| Symbol | File | Type | Notes |
|--------|------|------|-------|
| publish | Makefile | make target | depends on release; calls scripts/push-all.sh + gh release create |
| Installation section | README.md | documentation section | Releases download link + Gatekeeper bypass instructions |

<output>
Create `.planning/phases/05-format-expansion-distribution/05-03-SUMMARY.md` when done.
</output>
