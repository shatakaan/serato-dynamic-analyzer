---
phase: 05-format-expansion-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - python/tests/test_geob.py
  - python/tests/fixtures/minimal.m4a
  - python/analyze.py
  - SeratoDynamicAnalyzer/ViewModels/BatchViewModel.swift
  - SeratoDynamicAnalyzer/Views/BatchQueueView.swift
autonomous: false
requirements:
  - FMT-04
must_haves:
  truths:
    - "An M4A file dropped onto the app is accepted into the queue (not filtered out)"
    - "analyze.py decodes M4A audio via ffmpeg (no -vn) and detects beats correctly"
    - "write_geob_m4a() writes a parseable ----:com.serato.dj:beatgrid free-form atom"
    - "The stored atom value base64-decodes to a payload starting with the FLAC wrapper prefix"
    - "Serato DJ Pro reads the M4A BeatGrid tag and displays a correct beatgrid (D-13)"
    - "test_write_geob_m4a, test_m4a_geob_encoding_contract, test_m4a_atom_key all pass"
  artifacts:
    - path: "python/tests/fixtures/minimal.m4a"
      provides: "Binary fixture mutagen.mp4.MP4 can open for round-trip write tests"
    - path: "python/tests/test_geob.py"
      provides: "Tests 11, 13, 14 for M4A write round-trip, encoding contract, atom key"
    - path: "python/analyze.py"
      provides: "M4A decode branch in load_audio(), _write_geob_mp4_container(), write_geob_m4a(), MP4_BEATGRID_KEY constant"
    - path: "SeratoDynamicAnalyzer/ViewModels/BatchViewModel.swift"
      provides: "m4a added to extension filter in addDroppedURL() and scanFolder()"
    - path: "SeratoDynamicAnalyzer/Views/BatchQueueView.swift"
      provides: "UTType.mpeg4Audio added to allowedContentTypes in openFilePicker()"
  key_links:
    - from: "python/analyze.py :: _select_write_fn()"
      to: "write_geob_m4a()"
      via: "elif ext == '.m4a'"
      pattern: "elif ext == '.m4a'"
    - from: "write_geob_m4a()"
      to: "_write_geob_mp4_container()"
      via: "direct call"
      pattern: "_write_geob_mp4_container"
    - from: "_write_geob_mp4_container()"
      to: "mutagen.mp4.MP4"
      via: "af.tags[MP4_BEATGRID_KEY]"
      pattern: "MP4_BEATGRID_KEY"
---

## Phase Goal

**As a** DJ, **I want to** analyze M4A tracks and have the beatgrid written back into the file, **so that** I can use my M4A library in Serato with the same dynamic beatgrid quality I get for MP3 files.

<objective>
Deliver the M4A support vertical slice: failing tests written first (RED), then analyze.py decode/write implementation (GREEN), then Swift extension filter and file picker changes to accept .m4a, and finally a blocking Serato gate that confirms Serato DJ Pro reads the tag correctly.

Purpose: FMT-04 — App analyzes and writes M4A/AAC files via bundled ffmpeg. M4A is the dominant format in many DJ libraries (Apple Music exports, iTunes purchases).

Output: test_geob.py tests 11/13/14 green, minimal.m4a fixture committed, analyze.py M4A decode and write path live, SwiftUI drop handler and file picker accept .m4a, Serato gate PASSED.
</objective>

<execution_context>
@/Users/andreasmrogenda/.claude/gsd-core/workflows/execute-plan.md
@/Users/andreasmrogenda/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-format-expansion-distribution/05-CONTEXT.md
@.planning/phases/05-format-expansion-distribution/05-RESEARCH.md
@.planning/phases/05-format-expansion-distribution/05-PATTERNS.md
@.planning/phases/05-format-expansion-distribution/05-UI-SPEC.md
@.planning/phases/05-format-expansion-distribution/05-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED stubs + minimal.m4a fixture — test_geob.py tests 11, 13, 14</name>
  <files>python/tests/test_geob.py, python/tests/fixtures/minimal.m4a</files>

  <read_first>
    - python/tests/test_geob.py — full file: understand existing _make_minimal_mp3/aiff/wav helpers (lines 137–209) and test_6/test_9/test_10 write round-trip structure (lines 212–360) to replicate the same pattern
    - .planning/phases/05-format-expansion-distribution/05-PATTERNS.md — "test_geob.py — M4A/MP4 test cases" section: exact test code patterns for tests 11, 13, 14
    - .planning/phases/05-format-expansion-distribution/05-RESEARCH.md — "Minimal M4A test file for pytest" and "Open Question 1" and "Pitfall 6": understand minimal fixture construction challenge
    - .planning/phases/05-format-expansion-distribution/05-VALIDATION.md — Wave 0 Requirements section
  </read_first>

  <behavior>
    - test_write_geob_m4a (test 11): calls _make_minimal_m4a(), calls analyze.write_geob_m4a(), asserts '----:com.serato.dj:beatgrid' in mutagen.mp4.MP4(path).tags — MUST fail RED because write_geob_m4a does not exist yet
    - test_m4a_geob_encoding_contract (test 13): calls write_geob_m4a(), base64-decodes the stored atom value (padding added for decode), asserts decoded payload starts with b'application/octet-stream\x00\x00Serato BeatGrid\x00' — MUST fail RED
    - test_m4a_atom_key (test 14): calls write_geob_m4a(), reads keys from MP4 tags, asserts '----:com.serato.dj:beatgrid' in keys AND '----:com.serato.dj:Serato BeatGrid' NOT in keys — MUST fail RED
    - _make_minimal_m4a(path) helper: first attempt mutagen.mp4.MP4() constructor on new file; if that raises mutagen.mp4.error or MP4StreamInfoError, fall back to writing tests/fixtures/minimal.m4a bytes to the path using path.write_bytes()
  </behavior>

  <action>
  Create the minimal.m4a fixture first: run `cd python && venv/bin/python -c "import mutagen.mp4; af = mutagen.mp4.MP4(); af.save('/tmp/test_new.m4a')"` to test whether mutagen can construct a new MP4 file. If it succeeds and produces a valid file, commit that file as tests/fixtures/minimal.m4a. If it raises mutagen.mp4.error, construct minimal.m4a using the MINIMAL_M4A_BYTES approach from RESEARCH.md Pattern 3 (ftyp + moov binary). Create the fixtures/ directory if it does not exist.

  Add _make_minimal_m4a(path: Path) helper to test_geob.py below _make_minimal_wav (line ~209). The helper must: (1) try mutagen.mp4.MP4() constructor and call af.save(str(path)), (2) on any mutagen.mp4.error exception, fall back to path.write_bytes() reading from tests/fixtures/minimal.m4a relative to the tests directory.

  Add test_11_m4a_write, test_13_m4a_geob_encoding_contract, test_14_m4a_atom_key per PATTERNS.md patterns. Each test must import analyze from sys.path as the existing tests do (see top of test_geob.py for the sys.path.insert pattern). Use analyze.pack_beatgrid(non_terminal_markers=[(1.0, 8)], terminal_marker=(2.0, 120.0), footer_byte=analyze.GEOB_FOOTER) as the test payload.

  In test_13, decode the stored bytes as: stored = bytes(af.tags['----:com.serato.dj:beatgrid'][0]); decoded = base64.b64decode(stored + b'=='); assert decoded.startswith(b'application/octet-stream\x00\x00Serato BeatGrid\x00').

  Verify tests fail RED before proceeding to Task 2.
  </action>

  <verify>
    <automated>cd python && venv/bin/python -m pytest tests/test_geob.py::test_11_m4a_write tests/test_geob.py::test_13_m4a_geob_encoding_contract tests/test_geob.py::test_14_m4a_atom_key -x -q 2>&1 | grep -E "FAILED|ERROR|passed|failed"</automated>
  </verify>

  <acceptance_criteria>
    - tests/fixtures/minimal.m4a exists as a file with size > 0
    - Running the three new tests results in FAILED (not ERROR) — meaning the test ran, analyze was imported, but write_geob_m4a raised AttributeError or NameError (does not exist yet)
    - _make_minimal_m4a() is defined in test_geob.py and does not raise on a tmp_path fixture
    - No existing tests (tests 1–10) break: `cd python && venv/bin/python -m pytest tests/test_geob.py -x -q` shows same pass count as before (10+ tests passing, 3 new FAILED)
  </acceptance_criteria>

  <done>tests/fixtures/minimal.m4a committed, three new test stubs written in test_geob.py, stubs fail RED (FAILED not ERROR), all existing tests still pass. After this task completes, update 05-VALIDATION.md frontmatter: set nyquist_compliant: true and wave_0_complete: true.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — analyze.py M4A decode + write + dispatch (D-06, D-07, D-08)</name>
  <files>python/analyze.py</files>

  <read_first>
    - python/analyze.py — lines 1–60 (imports + ALLOWED_EXTENSIONS/SUPPORTED_EXTENSIONS), lines 63–130 (load_audio() full function with MP3 ffmpeg branch), lines 466–560 (write_geob_mp3/aiff/wav functions), lines 560–650 (atomic_write_geob()), lines 860–880 (_select_write_fn() and end of file)
    - .planning/phases/05-format-expansion-distribution/05-PATTERNS.md — "analyze.py — M4A/MP4 branches": exact import additions, ALLOWED_EXTENSIONS changes, load_audio M4A branch, _write_geob_mp4_container + write_geob_m4a code, _select_write_fn extension
    - .planning/phases/05-format-expansion-distribution/05-RESEARCH.md — "The M4A/MP4 GEOB Encoding Contract (Critical)" section: exact encoding logic, atom key, FLAC wrapper, base64 no-padding requirement
  </read_first>

  <behavior>
    - load_audio('.m4a'): cmd = [ffmpeg_exe, '-i', str(path), '-f', 'wav', '-ar', '22050', '-ac', '1', 'pipe:1'] — no -vn flag (D-06); raises RuntimeError with 500-char stderr excerpt on non-zero returncode
    - _write_geob_mp4_container(path, geob_bytes): wrapper = b'application/octet-stream' + b'\x00\x00' + b'Serato BeatGrid' + b'\x00' + geob_bytes; encoded = base64.b64encode(wrapper).rstrip(b'='); af.tags[MP4_BEATGRID_KEY] = [MP4FreeForm(encoded, dataformat=AtomDataType.IMPLICIT)]; af.save()
    - write_geob_m4a(path, geob_bytes): delegates to _write_geob_mp4_container(path, geob_bytes) — no additional logic
    - ALLOWED_EXTENSIONS and SUPPORTED_EXTENSIONS: both frozenset/set gain '.m4a' (D-08)
    - _select_write_fn(): adds elif ext == '.m4a': return write_geob_m4a before the final else raise
  </behavior>

  <action>
  Add the following imports at the top of analyze.py alongside existing mutagen imports (per PATTERNS.md "Imports pattern"): import base64, import mutagen.mp4, from mutagen.mp4 import MP4FreeForm, AtomDataType.

  Update ALLOWED_EXTENSIONS (line ~52) and SUPPORTED_EXTENSIONS (line ~56) to include '.m4a' in both sets (D-08).

  In load_audio(), add elif path.suffix.lower() == '.m4a' branch after the MP3 block. The branch is identical to the MP3 branch except no -vn flag (D-06). cmd list must be constructed with list syntax (no shell=True) — T-05-02 mitigation, same as MP3 branch.

  Add MP4_BEATGRID_KEY = '----:com.serato.dj:beatgrid' constant after write_geob_wav (per PATTERNS.md). Add _write_geob_mp4_container(path, geob_bytes) shared helper using the exact encoding from RESEARCH.md: FLAC wrapper construction, base64.b64encode().rstrip(b'='), mutagen.mp4.MP4 open-modify-save idiom. Add write_geob_m4a(path, geob_bytes) that calls _write_geob_mp4_container (per D-07, D-05).

  Extend _select_write_fn() to add the M4A case (per PATTERNS.md "Dispatch function pattern"). Insert elif ext == '.m4a': return write_geob_m4a before the else raise.

  Atomic_write_geob() requires no changes — it is format-agnostic (write_fn is passed in from _select_write_fn).
  </action>

  <verify>
    <automated>cd python && venv/bin/python -m pytest tests/test_geob.py::test_11_m4a_write tests/test_geob.py::test_13_m4a_geob_encoding_contract tests/test_geob.py::test_14_m4a_atom_key -x -q</automated>
  </verify>

  <acceptance_criteria>
    - All three new M4A tests pass GREEN
    - Full test suite still passes: `cd python && venv/bin/python -m pytest tests/test_geob.py -x -q` shows 14+ tests passing, 0 failing
    - `python -c "import analyze; print(analyze.ALLOWED_EXTENSIONS)"` output includes '.m4a'
    - `python -c "import analyze; print(analyze.MP4_BEATGRID_KEY)"` prints '----:com.serato.dj:beatgrid'
    - `grep -c 'write_geob_m4a\|_write_geob_mp4_container\|MP4_BEATGRID_KEY' python/analyze.py` returns at least 6 (definitions + call sites)
    - `grep 'elif.*m4a' python/analyze.py | wc -l` returns at least 2 (load_audio + _select_write_fn)
  </acceptance_criteria>

  <done>analyze.py M4A decode + write + dispatch live, 3 new tests GREEN, all existing tests still pass</done>
</task>

<task type="auto">
  <name>Task 3: Swift extension filter + file picker — accept .m4a (D-08, FMT-04)</name>
  <files>SeratoDynamicAnalyzer/ViewModels/BatchViewModel.swift, SeratoDynamicAnalyzer/Views/BatchQueueView.swift</files>

  <read_first>
    - SeratoDynamicAnalyzer/ViewModels/BatchViewModel.swift — lines 155–195: addDroppedURL() at line 158 (guard at line 169), scanFolder() at line 176 (if-contains at line 186). Both locations must be updated.
    - SeratoDynamicAnalyzer/Views/BatchQueueView.swift — lines 89–105: openFilePicker() allowedContentTypes array. Current array ends after UTType wav entry.
    - .planning/phases/05-format-expansion-distribution/05-UI-SPEC.md — "Change 1" and "Change 2" sections: prescriptive change contract with before/after code
    - .planning/phases/05-format-expansion-distribution/05-PATTERNS.md — "BatchViewModel.swift — extension filter additions" and "BatchQueueView.swift — UTType additions" sections
  </read_first>

  <action>
  In BatchViewModel.swift, update TWO independent extension filter lists (both must change — RESEARCH.md Pitfall 7):
  Location A (addDroppedURL, line 169): change guard ["mp3", "aiff", "aif", "wav"].contains(ext) to guard ["mp3", "aiff", "aif", "wav", "m4a", "mp4"].contains(ext) — note mp4 is included here even though MP4 plan is separate, since both extensions are in the same list and updating once avoids a second Swift build cycle.
  Location B (scanFolder, line 186): change if ["mp3", "aiff", "aif", "wav"].contains(ext) to if ["mp3", "aiff", "aif", "wav", "m4a", "mp4"].contains(ext).

  In BatchQueueView.swift openFilePicker() (line 94–99), append two UTType entries to allowedContentTypes after the existing wav entry:
  UTType.mpeg4Audio  (with comment // .m4a — FMT-04)
  UTType.mpeg4Movie  (with comment // .mp4 — FMT-05)
  No import changes needed — UniformTypeIdentifiers is already imported.

  Build the app to verify no Swift compile errors: DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project SeratoDynamicAnalyzer/SeratoDynamicAnalyzer.xcodeproj -scheme SeratoDynamicAnalyzer -configuration Release -quiet build 2>&1 | tail -5.
  </action>

  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project SeratoDynamicAnalyzer/SeratoDynamicAnalyzer.xcodeproj -scheme SeratoDynamicAnalyzer -configuration Release -quiet 2>&1 | tail -3</automated>
  </verify>

  <acceptance_criteria>
    - BUILD SUCCEEDED (last line of xcodebuild output)
    - `grep -c 'm4a.*mp4\|mp4.*m4a' SeratoDynamicAnalyzer/ViewModels/BatchViewModel.swift` returns at least 2 (both locations updated)
    - `grep 'mpeg4Audio' SeratoDynamicAnalyzer/Views/BatchQueueView.swift` returns 1 match
    - `grep 'mpeg4Movie' SeratoDynamicAnalyzer/Views/BatchQueueView.swift` returns 1 match
    - `grep '\.m4a\|m4a' SeratoDynamicAnalyzer/ViewModels/BatchViewModel.swift | grep -v '//' | wc -l` returns 2 (both guard and if-contains lines)
  </acceptance_criteria>

  <done>BatchViewModel and BatchQueueView accept .m4a (and .mp4) extensions; Swift BUILD SUCCEEDED</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Serato gate — M4A BeatGrid tag verification (D-13)</name>

  <what-built>
  Tasks 1–3 delivered: test stubs, analyze.py M4A decode and write path, Swift drop handler and file picker accepting .m4a. The Python pipeline now decodes M4A via ffmpeg, detects beats, packs the BeatGrid binary, wraps it in the FLAC prefix, base64-encodes without padding, and writes it as a ----:com.serato.dj:beatgrid free-form atom using mutagen.mp4.MP4.
  </what-built>

  <how-to-verify>
  1. Open Terminal. Run the app: open build/SeratoDynamicAnalyzer.app (or build with make and run).
  2. Drag a real .m4a file (an M4A track from your music library) onto the batch queue.
  3. Confirm the file appears in the queue (not filtered). Click Analyze All.
  4. Wait for analysis to complete. Confirm the track shows Done status with a BPM value.
  5. Confirm a .serato-backup.m4a file was created alongside the original.
  6. Open the analyzed .m4a file in Serato DJ Pro.
  7. Navigate to the beatgrid view. Verify the beatgrid is visible, correctly positioned, and snapped to beats (does not drift over the track).
  8. Optional sanity check: run `cd python && venv/bin/python -c "import mutagen.mp4; af = mutagen.mp4.MP4('/path/to/your/track.m4a'); print(list(af.tags.keys()))"` and confirm '----:com.serato.dj:beatgrid' is present.
  </how-to-verify>

  <resume-signal>Type "M4A gate passed" if Serato displays the beatgrid correctly. Type "M4A gate failed: [describe issue]" if the beatgrid is missing or incorrect.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied M4A path → analyze.py | File path from drag-and-drop or picker crosses into ffmpeg subprocess invocation |
| ffmpeg stdout → soundfile.read | Raw audio bytes from subprocess pipe are treated as WAV data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | analyze.py ALLOWED_EXTENSIONS + Path.resolve() | mitigate | Extend existing ALLOWED_EXTENSIONS frozenset to include '.m4a'; Path.resolve() already called in analyze_track() before extension check — no new code path needed (per D-08) |
| T-05-02 | Tampering | load_audio() M4A ffmpeg subprocess | mitigate | cmd constructed as a Python list (no shell=True, no string interpolation); identical pattern to existing MP3 branch — T-02-03 mitigation already enforced; M4A branch copies it verbatim |
| T-05-03 | Spoofing | Ad-hoc signed DMG distribution | accept | Ad-hoc signing per D-01; README Gatekeeper bypass instructions per RESEARCH.md Discretion section; users are developers/DJs who understand one-time override — risk accepted |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages in Phase 5; mutagen and imageio-ffmpeg already installed in prior phases; Package Legitimacy Audit table in RESEARCH.md confirms no new installs |
</threat_model>

<verification>
1. All Python tests pass: `cd python && venv/bin/python -m pytest tests/ -x -q` — 0 failures
2. New M4A tests (11, 13, 14) are individually runnable and GREEN
3. Swift BUILD SUCCEEDED for SeratoDynamicAnalyzer
4. M4A Serato gate PASSED (checkpoint:human-verify Task 4)
5. ALLOWED_EXTENSIONS includes '.m4a': `python -c "import sys; sys.path.insert(0,'python'); import analyze; assert '.m4a' in analyze.ALLOWED_EXTENSIONS"`
6. MP4_BEATGRID_KEY constant is exactly '----:com.serato.dj:beatgrid': `python -c "import sys; sys.path.insert(0,'python'); import analyze; assert analyze.MP4_BEATGRID_KEY == '----:com.serato.dj:beatgrid'"`
</verification>

<success_criteria>
- FMT-04 implemented: M4A/AAC files are decoded via bundled ffmpeg and receive correct Serato BeatGrid tags
- test_write_geob_m4a, test_m4a_geob_encoding_contract, test_m4a_atom_key: all GREEN (pytest exit 0)
- Serato DJ Pro reads the M4A BeatGrid tag without error (D-13 gate passed)
- BatchViewModel and BatchQueueView accept .m4a files via drag-and-drop, folder scan, and file picker
- No regressions: full test suite (tests/test_geob.py + all other test files) still passes
</success_criteria>

## Artifacts This Phase Produces

| Symbol | File | Type | Notes |
|--------|------|------|-------|
| MP4_BEATGRID_KEY | python/analyze.py | module constant | str '----:com.serato.dj:beatgrid' |
| _write_geob_mp4_container | python/analyze.py | function | shared helper for M4A and MP4 |
| write_geob_m4a | python/analyze.py | function | delegates to _write_geob_mp4_container |
| ALLOWED_EXTENSIONS | python/analyze.py | frozenset | extended to include .m4a (and .mp4 in Plan 02) |
| SUPPORTED_EXTENSIONS | python/analyze.py | set | extended to include .m4a (and .mp4 in Plan 02) |
| _make_minimal_m4a | python/tests/test_geob.py | test helper | creates minimal MP4 container for fixture tests |
| test_11_m4a_write | python/tests/test_geob.py | test function | mutagen round-trip for write_geob_m4a |
| test_13_m4a_geob_encoding_contract | python/tests/test_geob.py | test function | FLAC wrapper + base64 contract |
| test_14_m4a_atom_key | python/tests/test_geob.py | test function | atom key lowercase assertion |
| tests/fixtures/minimal.m4a | python/tests/fixtures/minimal.m4a | binary fixture | mutagen-parseable minimal M4A container |

<output>
Create `.planning/phases/05-format-expansion-distribution/05-01-SUMMARY.md` when done.
</output>
