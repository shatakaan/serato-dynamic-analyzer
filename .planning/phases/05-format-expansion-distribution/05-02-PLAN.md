---
phase: 05-format-expansion-distribution
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - python/tests/test_geob.py
  - python/analyze.py
autonomous: false
requirements:
  - FMT-05
must_haves:
  truths:
    - "An MP4 file dropped onto the app is accepted into the queue (addDroppedURL + scanFolder already updated in Plan 01)"
    - "analyze.py decodes MP4 audio via ffmpeg with -vn flag and detects beats"
    - "write_geob_mp4() writes a parseable ----:com.serato.dj:beatgrid free-form atom"
    - "test_write_geob_mp4 passes GREEN"
    - "Serato DJ Pro reads the MP4 BeatGrid tag and displays a correct beatgrid (D-13)"
  artifacts:
    - path: "python/tests/test_geob.py"
      provides: "Test 12 for MP4 write round-trip via write_geob_mp4"
    - path: "python/analyze.py"
      provides: "MP4 decode branch in load_audio() with -vn flag, write_geob_mp4() function, .mp4 in ALLOWED_EXTENSIONS + SUPPORTED_EXTENSIONS + _select_write_fn()"
  key_links:
    - from: "python/analyze.py :: _select_write_fn()"
      to: "write_geob_mp4()"
      via: "elif ext == '.mp4'"
      pattern: "elif ext == '.mp4'"
    - from: "write_geob_mp4()"
      to: "_write_geob_mp4_container()"
      via: "direct call — shared helper from Plan 01"
      pattern: "_write_geob_mp4_container"
---

## Phase Goal

**As a** DJ, **I want to** analyze MP4 video files and have the beatgrid written back into the file, **so that** video tracks in my library work in Serato with accurate dynamic beatgrids.

<objective>
Deliver the MP4 support vertical slice: RED stub for test_write_geob_mp4, then analyze.py MP4 decode (ffmpeg -vn) and write_geob_mp4 function (delegating to the shared _write_geob_mp4_container helper from Plan 01), then a blocking Serato gate.

Purpose: FMT-05 — App analyzes and writes MP4 files via bundled ffmpeg. MP4 is a video container; the audio stream is extracted with ffmpeg -vn.

Output: test_12_mp4_write GREEN, analyze.py MP4 load/write/dispatch complete, Serato gate PASSED for MP4.
</objective>

<execution_context>
@/Users/andreasmrogenda/.claude/gsd-core/workflows/execute-plan.md
@/Users/andreasmrogenda/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-format-expansion-distribution/05-CONTEXT.md
@.planning/phases/05-format-expansion-distribution/05-RESEARCH.md
@.planning/phases/05-format-expansion-distribution/05-PATTERNS.md
@.planning/phases/05-format-expansion-distribution/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED stub — test_12_mp4_write</name>
  <files>python/tests/test_geob.py</files>

  <read_first>
    - python/tests/test_geob.py — lines 220–260: test_11_m4a_write structure (added in Plan 01) to replicate for MP4
    - .planning/phases/05-format-expansion-distribution/05-PATTERNS.md — "test_12_mp4_write pattern" in test_geob.py section
  </read_first>

  <behavior>
    - test_write_geob_mp4 (test 12): calls _make_minimal_m4a() with a .mp4 extension tmp_path, calls analyze.write_geob_mp4(), asserts '----:com.serato.dj:beatgrid' in mutagen.mp4.MP4(path).tags — MUST fail RED because write_geob_mp4 does not yet exist
  </behavior>

  <action>
  Add test_12_mp4_write immediately after test_11_m4a_write in test_geob.py. Use _make_minimal_m4a() with tmp_path / 'test.mp4' — the same minimal fixture works for both M4A and MP4 containers since they share the MP4 box format. Call analyze.write_geob_mp4() with the test payload (same pack_beatgrid args as test 11). Assert '----:com.serato.dj:beatgrid' in mutagen.mp4.MP4(str(mp4_file)).tags.

  Verify the test fails RED: `cd python && venv/bin/python -m pytest tests/test_geob.py::test_12_mp4_write -x -q` must output FAILED (not ERROR).
  </action>

  <verify>
    <automated>cd python && venv/bin/python -m pytest tests/test_geob.py::test_12_mp4_write -x -q 2>&1 | grep -E "FAILED|ERROR|passed"</automated>
  </verify>

  <acceptance_criteria>
    - pytest output shows FAILED for test_12_mp4_write (not ERROR — test ran but write_geob_mp4 is missing)
    - All other tests in test_geob.py still pass (tests 1–11, 13–14 green): `cd python && venv/bin/python -m pytest tests/test_geob.py -x -q 2>&1 | grep -E "passed|failed"` shows 14 passed, 1 failed
  </acceptance_criteria>

  <done>test_12_mp4_write written and failing RED; all other tests still green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — analyze.py MP4 decode + write + dispatch (D-09, D-10, D-11, D-12)</name>
  <files>python/analyze.py</files>

  <read_first>
    - python/analyze.py — lines 63–130: load_audio() function; find the newly added .m4a branch (from Plan 01) to see its exact position and replicate the structure for .mp4 with -vn flag
    - python/analyze.py — lines ~466–600: find write_geob_m4a and _write_geob_mp4_container (added in Plan 01), and _select_write_fn() (lines ~863–880): understand current dispatch table
    - .planning/phases/05-format-expansion-distribution/05-PATTERNS.md — "load_audio MP4 branch" (elif mp4 block with -vn) and "Dispatch function pattern" (elif .mp4 case)
    - .planning/phases/05-format-expansion-distribution/05-RESEARCH.md — D-09, D-10, D-11, D-12 and "Pitfall 4: MP4 -vn Flag on M4A" (do NOT add -vn to M4A)
  </read_first>

  <behavior>
    - load_audio('.mp4'): cmd = [ffmpeg_exe, '-i', str(path), '-vn', '-f', 'wav', '-ar', '22050', '-ac', '1', 'pipe:1'] — explicit -vn flag to extract audio stream only (D-10); same RuntimeError on non-zero returncode; same soundfile.read(BytesIO) pattern
    - write_geob_mp4(path, geob_bytes): delegates to _write_geob_mp4_container(path, geob_bytes) — identical to write_geob_m4a; separate function for clarity in dispatch (D-11)
    - ALLOWED_EXTENSIONS and SUPPORTED_EXTENSIONS: add '.mp4' to both sets (D-12)
    - _select_write_fn(): add elif ext == '.mp4': return write_geob_mp4 after the M4A case
  </behavior>

  <action>
  In load_audio(), add elif path.suffix.lower() == '.mp4' branch after the .m4a branch. The command must include '-vn' as the second argument after '-i str(path)' to extract audio only (D-10). All other code is identical to M4A: same error handling, same soundfile.read, same mono channel collapse. cmd must remain a list (no shell=True) — T-05-02 mitigation.

  Add write_geob_mp4(path: Path, geob_bytes: bytes) function immediately after write_geob_m4a. Single line body: _write_geob_mp4_container(path, geob_bytes). Docstring: "Write Serato BeatGrid to MP4 file using MP4 free-form atom."

  Update ALLOWED_EXTENSIONS and SUPPORTED_EXTENSIONS to add '.mp4' (D-12). Note: BatchViewModel was already updated in Plan 01 to include 'mp4' in the Swift extension lists.

  Extend _select_write_fn() to add elif ext == '.mp4': return write_geob_mp4 after the M4A case.
  </action>

  <verify>
    <automated>cd python && venv/bin/python -m pytest tests/test_geob.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - All 15 tests in test_geob.py pass (including test_12_mp4_write): pytest exit 0
    - Full test suite passes: `cd python && venv/bin/python -m pytest tests/ -x -q` shows 0 failures
    - `python -c "import sys; sys.path.insert(0,'python'); import analyze; print(analyze.ALLOWED_EXTENSIONS)"` output includes '.mp4'
    - `grep 'elif.*mp4' python/analyze.py` returns at least 2 lines (load_audio + _select_write_fn)
    - `grep '\-vn' python/analyze.py` appears exactly once (in the .mp4 branch only — not in .m4a branch)
    - `grep 'write_geob_mp4' python/analyze.py | grep -v '#' | wc -l` returns at least 3 (definition, _select_write_fn return, and the delegate call body)
  </acceptance_criteria>

  <done>analyze.py MP4 decode + write + dispatch live, test_12_mp4_write GREEN, full test suite 0 failures</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Serato gate — MP4 BeatGrid tag verification (D-13)</name>

  <what-built>
  Tasks 1–2 delivered: test_12_mp4_write stub then GREEN, analyze.py MP4 decode path with -vn flag, write_geob_mp4 delegating to the shared _write_geob_mp4_container helper, .mp4 in ALLOWED_EXTENSIONS and _select_write_fn dispatch. BatchViewModel and BatchQueueView already accept .mp4 from Plan 01.
  </what-built>

  <how-to-verify>
  1. Open the app (open build/SeratoDynamicAnalyzer.app or rebuild with make).
  2. Drag a real .mp4 file (a video file containing audio, e.g., a DJ set recording or music video) onto the batch queue.
  3. Confirm the file appears in the queue (not filtered). Click Analyze All.
  4. Wait for analysis to complete. Confirm the track shows Done status with a BPM value.
  5. Confirm a .serato-backup.mp4 file was created alongside the original.
  6. Open the analyzed .mp4 file in Serato DJ Pro.
  7. Navigate to the beatgrid view. Verify the beatgrid is visible, correctly positioned, and snapped to beats.
  8. Optional CLI check: `cd python && venv/bin/python -c "import mutagen.mp4; af = mutagen.mp4.MP4('/path/to/your/track.mp4'); print([k for k in af.tags if 'serato' in k.lower()])"` — should print ['----:com.serato.dj:beatgrid'].
  </how-to-verify>

  <resume-signal>Type "MP4 gate passed" if Serato displays the beatgrid correctly. Type "MP4 gate failed: [describe issue]" if the beatgrid is missing or incorrect.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied MP4 path → analyze.py | File path from drag-and-drop crosses into ffmpeg subprocess invocation |
| ffmpeg -vn stdout → soundfile.read | Audio bytes extracted from video container treated as WAV data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | analyze.py ALLOWED_EXTENSIONS + Path.resolve() | mitigate | .mp4 added to ALLOWED_EXTENSIONS frozenset; Path.resolve() already called in analyze_track() — same gate as MP3/M4A (D-12) |
| T-05-02 | Tampering | load_audio() MP4 ffmpeg subprocess | mitigate | cmd constructed as a Python list (no shell=True); -vn is a positional list element, not a string interpolated with path — cannot be manipulated via a crafted filename |
| T-05-03 | Spoofing | Ad-hoc signed DMG distribution | accept | Handled in Plan 03; not in scope for this plan |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages; mutagen already verified in Phase 1 |
</threat_model>

<verification>
1. Full Python test suite: `cd python && venv/bin/python -m pytest tests/ -x -q` — 0 failures, all 15 test_geob tests pass
2. ALLOWED_EXTENSIONS includes .mp4: `python -c "import sys; sys.path.insert(0,'python'); import analyze; assert '.mp4' in analyze.ALLOWED_EXTENSIONS"`
3. -vn flag ONLY in .mp4 branch: `grep -n '\-vn' python/analyze.py` returns exactly one line, within the .mp4 elif block
4. MP4 Serato gate PASSED (checkpoint Task 3)
</verification>

<success_criteria>
- FMT-05 implemented: MP4 files are decoded via bundled ffmpeg with -vn and receive correct Serato BeatGrid tags
- test_12_mp4_write: GREEN (pytest exit 0)
- Serato DJ Pro reads the MP4 BeatGrid tag without error (D-13 gate passed)
- No regressions: full test suite still passes (0 failures)
</success_criteria>

## Artifacts This Phase Produces

| Symbol | File | Type | Notes |
|--------|------|------|-------|
| write_geob_mp4 | python/analyze.py | function | delegates to _write_geob_mp4_container (from Plan 01) |
| test_12_mp4_write | python/tests/test_geob.py | test function | mutagen round-trip for write_geob_mp4 |
| ALLOWED_EXTENSIONS | python/analyze.py | frozenset | extended to include .mp4 (Plan 01 added .m4a) |
| SUPPORTED_EXTENSIONS | python/analyze.py | set | extended to include .mp4 |

<output>
Create `.planning/phases/05-format-expansion-distribution/05-02-SUMMARY.md` when done.
</output>
